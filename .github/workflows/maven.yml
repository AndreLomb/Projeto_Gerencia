# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: |
        mvn clean install -DskipTests
    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: gerencia-artifact
        path: target/*.jar
  
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Run test
      run: |
        mvn clean test site
    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: reporte-de-testes
        path: target/site/
  
  send-email:

    needs: test

    runs-on: ubuntu-latest

    env:
      COMMIT_CODE: ${{ github.sha }}
      DESTINATARIO: ${{ vars.EMAIL_DESTINATARIO }} 
      
    steps:
      - name: Instalar mailutils
        run: |
          sudo apt update
          sudo apt install -y mailutils

      - name: Gerar link para a página do run (onde está o artifact)
        id: gerar-link
        run: |
          echo "url=https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" >> "$GITHUB_OUTPUT"

      - name: Enviar email com link do relatório
        env:
          REPORT_URL: ${{ steps.gerar-link.outputs.url }}
        run: |
          echo "Olá, Os testes passaram com sucesso. Commit: Relatório: Atenciosamente, GitHub Actions " "$COMMIT_CODE" "$REPORT_URL" \
            | mail -s "Relatório de Testes - Commit $COMMIT_CODE" "guiobrito3010@gmail.com"